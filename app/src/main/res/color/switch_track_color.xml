<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- When Switch is ON -->
    <item android:color="@color/navy_primary" android:state_checked="true"/>

    <!-- When Switch is OFF -->
    <item android:color="@color/filter_border" android:state_checked="false"/>

</selector>


<EditText
android:id="@+id/et_search_reports"
android:layout_width="match_parent"
android:layout_height="48dp"
android:hint="Search by name or email..."
android:paddingHorizontal="12dp"
android:background="@drawable/bg_input_field"
android:inputType="text"
android:drawableStart="@drawable/ic_nav_dashboard"
android:drawablePadding="8dp"
android:textSize="14sp"
android:textColor="@color/text_primary"
android:textColorHint="@color/text_tertiary"/>
    </LinearLayout>
