<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ritesh.hoppeconnect"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        No SEND_SMS / RECEIVE_SMS needed.
        Verification is done via Appwrite email — no device SIM required.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application


        android:forceDarkAllowed="false"
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hoppeconnect"
        android:usesCleartextTraffic="true"
        tools:targetApi="33">
        <activity android:name=".AdminReportsListActivity"/>
        <activity android:name=".AdminUsersListActivity"/>
        <activity android:name=".AdminAnnouncementActivity"/>
        <activity android:name=".AdminExportActivity"/>
        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Hoppeconnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity android:name=".ExploreActivity"  android:launchMode="singleTop" />
        <activity android:name=".ProfileActivity"  android:launchMode="singleTop" />
        <activity android:name=".ChatsActivity"    android:launchMode="singleTop" />

        <activity
            android:name=".MissedPersonDetailActivity"
            android:parentActivityName=".ExploreActivity" />
        <activity
            android:name=".NotificationsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MyReportsActivity"
            android:parentActivityName=".ProfileActivity" />

        <!-- Auth screens -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />

        <!-- Admin Dashboard -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Profile sub-screens -->
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".HelpSupportActivity" />
        <activity android:name=".PrivacyPolicyActivity" />
        <activity android:name=".AboutActivity" />

        <!-- Other screens -->
        <activity android:name=".NewReportActivity" />
        <activity android:name=".ChatRoomActivity" />
        <activity android:name=".ChatDetailActivity" />
        <activity android:name=".BuyTeaActivity" />
        <activity android:name=".TechnologyWithHumanityActivity" />
        <activity android:name=".NotificationActivity"/>

    </application>

</manifest>